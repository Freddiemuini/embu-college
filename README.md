# Embu College Website

A Flask-based college website with admin dashboard, course listings, news, notices, gallery, and student portal.

## Deployment

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run locally:
   ```bash
   python app.py
   ```

3. Production deploy uses `gunicorn` via `Procfile`.
